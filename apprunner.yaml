version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build
    post-build:
      - npm prune --omit=dev
run:
  command: node dist/server.js
  network:
    port: 3100
    env: PORT
  env:
    - name: HOST
      value: "0.0.0.0"
